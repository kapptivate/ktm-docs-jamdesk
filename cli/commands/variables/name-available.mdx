---
title: "ktm variables name-available"
description: "Pre-flight: check whether a variable name is free under a product"
sidebarTitle: "name-available"
---

{/* GENERATED FILE — do not edit by hand. Regenerate with: node scripts/generate.mjs */}

Pre-flight: check whether a variable name is free under a product

## Usage

```bash
ktm variables name-available [flags]
```

## Flags

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--name` | string | — | Variable name to check |
| `--product-id` | int | — | Product numeric id (from 'ktm products list') |

## Examples

```bash
ktm products list -o table       # find the product id
ktm variables name-available --product-id 134 --name MY_VAR
```

## Details

Returns "available" or "taken" so you can surface name conflicts client-side
before attempting create. Takes the product's NUMERIC ID (find via 'ktm products
list -o table'), not the slug.

Implementation note: the platform's check_variable_exist endpoint is
unreliable, so the check is performed client-side by listing the product's
variables and matching by name.

<Note>
  Global flags (`--output`, `--debug`, `--host`, …) apply to every command. See the [command reference overview](/cli/commands/overview).
</Note>

## What's Next?

<Columns cols={2}>
  <Card title="All commands" icon="terminal" href="/cli/commands/overview">
    Browse the full CLI reference.
  </Card>
  <Card title="Get started" icon="rocket" href="/cli/getting-started">
    Install the CLI and authenticate.
  </Card>
</Columns>
