---
title: Vérifier avec l'IA
sidebarTitle: Vérifier avec l'IA
description: Vérifiez n'importe quel aspect de la page courante en langage naturel, évalué par l'IA.
---

Vérifiez ce qu'un selector ne peut pas exprimer : "le graphique montre une tendance à la hausse", "le total du panier correspond à la somme des articles", "aucune bannière d'erreur n'est visible". L'IA observe l'écran courant et juge si votre assertion est vraie.

## Paramètres

| Paramètre | Champ JSON | Requis | Description |
|-----------|------------|--------|-------------|
| Assertion | `assertion` | Oui | Ce qui doit être vrai, en langage naturel. Par exemple : "Le panier affiche 2 articles et un total de 39,98 €". Supporte les variables. |
| Ignorer l'erreur sur ce step | `ignore_errors` | Non | L'exécution continue avec le step suivant si celui-ci échoue. Par défaut : désactivé. |

## Dans le JSON du scénario

Le step se trouve dans `step_groups[].steps` du scénario de test.

```json
{
  "id": "step-1",
  "type": "ai-visual-assert",
  "version": "v1.0.0",
  "disabled": false,
  "inputs": {
    "assertion": [{ "type": "value", "data": { "value": "The order confirmation banner is visible and mentions a delivery date" } }],
    "ignore_errors": false
  },
  "use_capabilities": false,
  "capabilities": {}
}
```

## Résultats et métriques

- **Temps** : début, fin et durée (en secondes) enregistrés dans le résultat du step.
- **Statut** : succès lorsque l'IA juge l'assertion vraie sur l'écran courant, échec dans le cas contraire, avec l'explication de l'IA dans le résultat.
- **Capture d'écran** : prise pour l'état vérifié, afin que vous puissiez examiner ce que l'IA a évalué.
- **Utilisation IA** : le coût IA de l'évaluation est ajouté à l'exécution.

Pour rédiger des assertions fiables, consultez [Vérifier avec l'IA](/fr/ai/verify-with-ai).

## Et ensuite ?

<Columns cols={2}>
  <Card title="Guide Vérifier avec l'IA" icon="sparkles" href="/fr/ai/verify-with-ai">

    Bonnes pratiques pour rédiger des assertions que l'IA évalue de manière fiable

</Card>
  <Card title="Vérifier un élément" icon="eye" href="/fr/tests/actions/web/verifyelement">

    Vérifications déterministes d'éléments, quand un selector suffit

</Card>
</Columns>
