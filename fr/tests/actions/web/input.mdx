---
title: Fill input
sidebarTitle: Saisie de texte
description: Saisir du texte dans un champ de formulaire ou une zone de texte.
---

Saisissez du texte dans un champ de la page. La valeur peut mélanger du texte fixe, des variables et des données générées, pour qu'un même step puisse se connecter avec un compte enregistré ou s'inscrire avec un e-mail aléatoire à chaque exécution.

## Paramètres

| Paramètre | Requis | Description |
| ----------- | -------- | ------------- |
| Élément cible | Oui, sauf si un selector manuel est défini | Description en langage naturel du champ. L'IA l'utilise pour trouver l'élément. |
| Selector manuel | Non | Selector XPath ou CSS, utilisé quand l'IA est désactivée ou comme selector enregistré. |
| Valeur | Oui | Texte à saisir. Supporte les variables et les fonctions comme le texte aléatoire. |
| Utilisation de l'IA | Non | `inherit` (par défaut, suit la stratégie du test), `smart` ou `always`. |
| Activer l'IA | Non | Quand désactivé, seul le selector manuel est utilisé. Par défaut : activé. |
| Ignorer l'erreur sur ce step | Non | L'exécution continue avec le step suivant si celui-ci échoue. Par défaut : désactivé. |

## Résultats et métriques

- **Temps d'exécution** : début, fin et durée (en secondes) enregistrés dans le résultat du step.
- **Statut** : succès, avertissement ou échec, avec le message d'erreur en cas d'échec.
- **Capture d'écran** : prise après l'exécution du step.
- **Utilisation de l'IA** : si l'IA a localisé l'élément, le selector corrigé est enregistré sur le test et le coût IA est ajouté à l'exécution.

## Et ensuite ?

<Columns cols={2}>
  <Card title="Press keys" icon="keyboard" href="/fr/tests/actions/web/keys">

    Appuyer sur Entrée ou Tab après la saisie

</Card>
  <Card title="Variables" icon="brackets-curly" href="/fr/tests/variables">

    Tout ce que vous pouvez insérer dans la valeur : variables, texte aléatoire et plus encore

</Card>
</Columns>
