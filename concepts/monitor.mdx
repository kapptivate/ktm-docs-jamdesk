---
title: Monitor
description: A test promoted to continuous production supervision on a recurring schedule.
icon: gauge
---

A **Monitor** is a [test](/concepts/test) promoted to production continuous supervision. Instead of running once and stopping, a monitor runs on a recurring schedule (hourly, daily, weekly, etc.) and tracks the application's health over time.

Monitors have statuses: up (healthy), degraded (partial failure), or down (critical failure). When a monitor's status changes unexpectedly, it triggers an **Incident**, an alert you can see in the Overview and respond to.

Monitors are how you catch production issues before your users do. Create a monitor from any test once it's proven reliable.
