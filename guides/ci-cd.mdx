---
title: CI/CD setup
description: "Run automated checks in your CI pipeline."
---

<Tip>
Placeholder — add GitHub Actions, GitLab CI, and CircleCI examples.
</Tip>

## GitHub Actions

```yaml
name: Checks
on: [push]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npx your-cli run
```
