---
title: Right click
description: Right-click an element to open its context menu.
---

Right-click an element to open a context menu or trigger any handler bound to the `contextmenu` event. Pair it with a [Click](/tests/actions/web/click) step to pick an entry from the menu that opens.

## Parameters

| Parameter | JSON field | Required | Description |
|-----------|------------|----------|-------------|
| Target element | `locator.description` | Yes, unless a manual selector is set | Natural-language description of the element. The AI uses it to find the element. |
| Manual selector | `locator.selector` | No | XPath or CSS selector, used when AI is disabled or as the saved selector. |
| AI usage | `ai_strategy` | No | `inherit` (default, follows the test strategy), `smart`, or `always`. |
| Enable AI | `enable_ai` | No | When off, only the manual selector is used. Default: on. |
| Ignore error on this step | `ignore_errors` | No | The run continues with the next step if this one fails. Default: off. |

## In the scenario JSON

The step lives in `step_groups[].steps` of the test scenario.

```json
{
  "id": "step-1",
  "type": "right-click",
  "version": "v1.0.0",
  "disabled": false,
  "inputs": {
    "locator": {
      "selector": [{ "type": "value", "data": { "value": "" } }],
      "description": [{ "type": "value", "data": { "value": "The first file in the attachments list" } }]
    },
    "enable_ai": true,
    "ai_strategy": "inherit",
    "ignore_errors": false
  },
  "use_capabilities": false,
  "capabilities": {}
}
```

## Results and metrics

- **Timing**: start, end, and duration (in seconds) recorded in the step result.
- **Status**: success, warning, or failed, with the error message on failure.
- **Screenshot**: captured after the right click, usually showing the opened context menu.
- **AI usage**: if the AI located the element, the healed selector is saved on the test and the AI cost is added to the run.

## What's Next?

<Columns cols={2}>
  <Card title="Click" icon="arrow-pointer" href="/tests/actions/web/click">

    Select an entry in the context menu you just opened

</Card>
  <Card title="Press keys" icon="keyboard" href="/tests/actions/web/keys">

    Close the menu or navigate it from the keyboard

</Card>
</Columns>
